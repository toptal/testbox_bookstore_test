docker:
  build:
  workdir: /test
scripts:
  start: tail -f /dev/null
  ls: ls
  parameterized: .testbox/parameterized.sh
patches:
  - name: bookstore-api-tests
    operations:
      - op: replace
        path: type
        value: job