docker:
  build:
  workdir: /test
scripts:
  start: tail -f /dev/null
  ls: ls
  parameterized: .testbox/parameterized.sh
patches:
  - name: kubernetes_type_job
    operations:
      - op: replace
        path: type
        value: job
