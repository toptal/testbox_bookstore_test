docker:
  build:
  workdir: /test
scripts:
  start: <%= ENV.fetch('BOOKSTORE_TEST_COMMAND') %>
  ls: ls
  parameterized: .testbox/parameterized.sh
patches:
  - name: bookstore-api-tests
    operations:
      - op: replace
        path: type
        value: job
